const api = 'http://ideadeploy.space/test'

export const rests = {
    rest_login : `${api}/login.json`,
    rest_info  : `${api}/info.json`,
    rest_graph : `${api}/graph.json`,
    rest_donut : `${api}/donut.json`,
}